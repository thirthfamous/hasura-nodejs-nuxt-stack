CREATE TABLE "public"."keuangan_transaksi"("id" serial NOT NULL, "type" integer NOT NULL, "nama_pembayaran" text NOT NULL, "jumlah" integer NOT NULL, "balance_now" integer NOT NULL, "created_by" uuid NOT NULL, "created_at" timestamptz NOT NULL DEFAULT now(), PRIMARY KEY ("id") , FOREIGN KEY ("created_by") REFERENCES "public"."user_karyawan"("id") ON UPDATE restrict ON DELETE restrict);
